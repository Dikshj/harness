services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite+aiosqlite:////data/harness.db
      - RLM_MEMORY_PATH=/data/.rlm_memory.jsonl
    volumes:
      - rlm_data:/data

volumes:
  rlm_data:
